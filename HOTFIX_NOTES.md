# SMPR 1.0.0rc3 hotfix

## rc3：V7 预处理修复

`1.0.0rc2` 的 V5/V6 核心和既有复核结果仍然有效，但不能用于准备 V7：
预注册清单中的 `cc_n12` 与 `qec9xz_n17` 含中途测量，不属于静态路由范围。
由于问题在冻结前、查看任何 V7 路由结果前被发现，rc3 在保持同一 QASMBench
提交、10 条线路、heavy-hex 规则、算法参数、运行预算和接受标准不变的前提下，
用静态线路 `dnn_n16` 与 `qram_n20` 替换它们。

rc3 还把 V7 准备改为事务式流程：下载、哈希校验、静态检查与规范化先在临时
目录完成，全部成功后才发布 `v7/sources`、`v7/normalized`、V7 数据集和配置。
失败时不会留下可被误冻结的正式半成品。

## rc2：公开核心组合修复

`1.0.0rc1` 不能用于生成正式结果，原因有二：

1. 公开路由入口保留了研发期双模式开关，默认进入仅含 LightSABRE 与独立展开
   的兼容路径，未执行跨布局候选；五例冒烟因此得到 191 而不是冻结值 182。
2. `doctor` 和发布测试只排除了名称恰为 `.venv` 的目录，安装在
   `.venv-smpr-public` 后会扫描第三方 `site-packages` 并产生伪命名泄漏。

rc2 删除研发期模式开关，公开入口始终运行完整 SMPR 组合；默认参数固定为
20 个 LightSABRE 种子、全部并列最优布局、每布局 4 个邻居和 0.95 代理衰减。
命令行同时接受规范连字符参数和旧兼容拼写，但所有新内部调用使用规范拼写。

命名检查现在排除虚拟环境、构建产物、安装元数据、缓存和结果目录，只审计
发布源码与静态资源。新增静态回归测试保证公开入口不会再次退回不完整组合。

rc2/rc3 共同的必要 V5 冒烟验收结果是：

```text
V5 smoke cases: 5
LightSABRE total SWAP: 191
SMPR total SWAP: 182
audits: 5 5 5
quality mismatches: 0
```
